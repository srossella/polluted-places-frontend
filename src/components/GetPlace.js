import React from 'react';
import Place from './Place';

function GetPlace(props) {
    return (
        <div>
            this is to retrieve place 
            <Place/>
        </div>
    );
}

export default GetPlace;